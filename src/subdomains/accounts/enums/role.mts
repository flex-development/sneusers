/**
 * @file Enums - Role
 * @module sneusers/accounts/enums/Role
 */

/**
 * User roles.
 *
 * @enum {Lowercase<string>}
 */
enum Role {
  DEVELOPER = 'developer',
  USER = 'user'
}

export default Role
