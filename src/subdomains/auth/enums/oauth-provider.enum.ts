/**
 * @file Auth Subdomain Enums - OAuthProvider
 * @module sneusers/subdomains/auth/enums/OAuthProvider
 */

/**
 * OAuth providers.
 *
 * @enum {Lowercase<string>}
 */
enum OAuthProvider {
  GITHUB = 'github',
  GOOGLE = 'google'
}

export default OAuthProvider
