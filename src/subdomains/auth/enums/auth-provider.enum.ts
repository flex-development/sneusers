/**
 * @file Auth Subdomain Enums - AuthProvider
 * @module sneusers/subdomains/auth/enums/AuthProvider
 */

/**
 * Authentication providers.
 *
 * @enum {Lowercase<string>}
 */
enum AuthProvider {
  GITHUB = 'github'
}

export default AuthProvider
