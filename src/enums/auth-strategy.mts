/**
 * @file Enums - AuthStrategy
 * @module sneusers/enums/AuthStrategy
 */

/**
 * Authentication strategies.
 *
 * @enum {Lowercase<string>}
 */
const enum AuthStrategy {
  JWT = 'jwt'
}

export default AuthStrategy
