/**
 * @file Enums - Subdomain
 * @module sneusers/enums/Subdomain
 */

/**
 * Subdomain names.
 *
 * @enum {Lowercase<string>}
 */
enum Subdomain {
  DOCS = 'docs',
  HEALTH = 'health'
}

export default Subdomain
