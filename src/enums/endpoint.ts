/**
 * @file Enums - Endpoint
 * @module sneusers/enums/Endpoint
 */

/**
 * API endpoints.
 *
 * @enum {Lowercase<string>}
 */
enum Endpoint {
  DOCS = '/',
  HEALTH = '/health'
}

export default Endpoint
